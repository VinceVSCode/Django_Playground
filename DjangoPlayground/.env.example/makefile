# === Django ===
# setup your Django settings here
# Strong, random key in production; not required in dev if you keep DEBUG=true.
DJANGO_SECRET_KEY=replace-me

# "true"/"false" (string). In production, set to "false".
DJANGO_DEBUG=true

# Comma-separated hostnames. For local dev without a domain, this is fine:
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# === Database (dev defaults to SQLite; leave these empty unless using Postgres/MySQL) ===
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=
DB_USER=
DB_PASS=
DB_HOST=
DB_PORT=

# === DRF/Auth/etc. (no required vars here right now) ===
# Add any required environment variables for your Django apps here.